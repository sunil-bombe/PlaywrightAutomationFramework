import * as allure from "allure-js-commons";

export class AllureUtility {
   addEnvironmentInfo(key: string, value: string) {
  }

   addAttachment(name: string, content: Buffer | string, type: string) {
    
  }

   addStep(name: string, status: 'passed' | 'failed' | 'broken' | 'skipped' | 'unknown') {

  }        

}